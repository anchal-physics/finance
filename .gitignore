*.xlsx
*.clasp.json
.DS_Store
*.com.json

# Google service-account key (never commit)
service_account.json

# Local secrets / config (sheet id, drive folder id, …) — never commit
.env
__pycache__/
.venv/

# All personal data files
data/
